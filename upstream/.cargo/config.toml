# Native-source registry routing; user-wide and legacy frontend settings are unchanged.
[source.crates-io]
replace-with = "official-github"

[source.official-github]
registry = "sparse+https://raw.githubusercontent.com/rust-lang/crates.io-index/master/"

[http]
multiplexing = false
timeout = 45
